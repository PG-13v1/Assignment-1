# Assignment-1

For Task 1.1 (directory : environment_replicate):
  If the makefile is in the same directory run:
    make install
  If the makefile is in another directory run:
    make -f {path_to_the _makefile} install

For Task 1.2 (directory : OOPS_design):
  Running the acceptance criterias as mentioned in the excel file

For Task 1.3 , Task 1.4 , Task 1.5 (directory: fastapi_modules):

Running 
  
  

  
